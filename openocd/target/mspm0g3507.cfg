# MSPM0G3507 target placeholder for OpenOCD.
# Replace with the OpenOCD target script supplied by the installed MSPM0/OpenOCD package.
# The EIDE OpenOCD profile points here so the CMSIS-DAP selection is explicit.
